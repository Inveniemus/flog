import 'package:flutter/material.dart';

class FlogApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('Flog'),
        ),
        body: Text('Test2'),
      ),
    );
  }
}
